
name: example-workflow
on: [push]

env:
  TARGET_BRANCH: ${{ github.base_ref }}

jobs:
  production-deploy:
    if: github.ref == 'production'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: This is Production
      - run: echo $TARGET_BRANCH



